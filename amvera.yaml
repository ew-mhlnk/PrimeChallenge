meta:
  environment: python
  toolchain:
    name: pip
    version: 3.11

build:
  # Путь к файлу зависимостей именно для бота
  requirementsPath: backend/bot/requirements.txt

run:
  persistenceMount: /data
  containerPort: 8080
  # Путь к файлу запуска
  command: python backend/bot/bot.py